Optimize MazeController. Output ONLY valid Python (no prose, markdown, # comments, docstrings, or ``` fences).

Replace code between ###optimization-start / ###optimization-end only. That block is a whole runnable file slice — output COMPLETE code (imports + class + helpers), not partial snippets, ellipses, or "..." placeholders. Do NOT output markers, prior feed, or anything outside the block.

API (signatures fixed):
- __init__(self, maze, start=(0, 0), max_steps=450)
- export(self) -> list[str]  # full path e.g. ["up","right",...]; build in export(), return self.moves — not one move per call

MAZE: NumPy 21×21; even row/col open (0), odd may be wall (1). Moves: up/down/left/right. 3×3 visibility on ball. Max 450 steps. Every open cell discovered once (enters 3×3 view).

PRIORITIES:
1. Discover ALL reachable open cells in FEWEST steps (maximize new cells / steps).
2. Minimize SOURCE size without omitting logic: short names, dense syntax, no comments/blanks; single-letter vars OK; inline over helpers if shorter; drop dead code only; deque/sets/tuples OK. Full discovery + working controller required.
3. Each iteration try a different approach (A*, Weighted A*, D* Lite, JPS, frontier BFS, info gain, best-first, hybrids) — fewest steps wins, not readability.
4. Must not crash: bounds checks, safe neighbors, fallback when no frontier.
5. Feedback — "code score" is main signal when not null; raise each good iter:
{feedback_text}
{error_section}{score_section}

ALLOWED: import any needed module (stdlib or pip) at top of block — e.g. `from collections import deque` if using deque; full rewrite; helpers only if net shorter.
FORBIDDEN: change __init__/export signatures; code outside block; non-Python output.

Shape: class MazeController with __init__(maze, start, max_steps) and export() returning full ["up"|"down"|"left"|"right"] list — implement both fully.

REFERENCE (do not copy):
{block_code}

Response = ONLY a complete optimized block (all imports + full MazeController). No partial code. Max compression. Full discovery. Fewest steps.
